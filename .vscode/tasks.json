{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "${workspaceFolder}/run_build.sh"
        },
        {
            "label": "test",
            "type": "shell",
            "command": "${workspaceFolder}/run_tests.sh"
        },
        {
            "label": "docs",
            "type": "shell",
            "command": "${workspaceFolder}/run_docs.sh"
        }
    ]
}